server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /armonia {
        return 301 /armonia/;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
